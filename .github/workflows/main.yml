name: Deploy Shared Hosting (Fix Connection)

on:
  push:
    branches:
      - main

jobs:
  deploy-hosting:
    name: 🚀 Build & Deploy
    runs-on: ubuntu-latest
    
    steps:
      - name: 🚚 Get Latest Code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 🔨 Install & Build Frontend
        run: |
          cd frontend
          rm -rf package-lock.json node_modules
          npm install --legacy-peer-deps
          npm install ajv@8.12.0 ajv-keywords@5.1.0 --save-dev --legacy-peer-deps
          CI=false npm run build

      - name: 📝 Prepare Backend Files
        run: |
          cd backend
          echo "DATABASE_URL=mysql+pymysql://prow9975_adminjates_db:jates1110@localhost/prow9975_jates9_db" > .env
          echo "JWT_SECRET_KEY=rahasia_jates_super_aman_2025" >> .env
          echo "WHATSAPP_API_KEY=HHHI0XGX5GAV9COH" >> .env
          echo "WHATSAPP_NUMBER_KEY=Hyw8OdnzTHwbi88t" >> .env
          echo "WHATSAPP_SERVICE_URL=https://api.watzap.id/v1/send_message" >> .env
          echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" >> .env
          
          if [ ! -f passenger_wsgi.py ]; then
            echo "import sys, os" > passenger_wsgi.py
            echo "from main import app as application" >> passenger_wsgi.py
          fi

      # Upload Frontend
      - name: 📂 Upload Frontend to Public HTML
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          # SAYA GANTI KE IP LANGSUNG AGAR LEBIH STABIL
          server: 203.175.9.105
          username: bimsrama@jagatetapsehat.com
          password: kuyen001
          local-dir: ./frontend/build/
          server-dir: ./
          # Hapus baris 'delete' yang error, ganti dengan logic standar
          exclude: |
            **/.git*
            backend_api/**
            cgi-bin/**
            .well-known/**
            .htaccess
          # Tambah timeout agar tidak cepat putus
          timeout: 30000

      # Upload Backend
      - name: 📂 Upload Backend to API Folder
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 203.175.9.105
          username: bimsrama@jagatetapsehat.com
          password: kuyen001
          local-dir: ./backend/
          server-dir: ./backend_api/
          exclude: |
            **/node_modules/**
            **/__pycache__/**
            **/venv/**
            **/*.pyc
            **/emergent/**
            *emergent*
          timeout: 30000
